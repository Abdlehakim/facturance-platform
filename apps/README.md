# Apps

Application folders are reserved for future Git submodules. Each app will be managed in its own repository and mounted here intentionally.

Do not initialize frameworks or add application code in these folders before the submodule repositories are created.
