# Services

Service folders are reserved for future Git submodules. Each backend service or worker will be managed in its own repository and mounted here intentionally.

Do not initialize frameworks or add service code in these folders before the submodule repositories are created.
